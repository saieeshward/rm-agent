# Runtime deps for the deployed web app (no Playwright/Chromium — the hosted DB
# is loaded from the cached snapshot, so the container never scrapes).
psycopg[binary]>=3.1
psycopg_pool>=3.2
pyyaml>=6.0

fastapi>=0.110
uvicorn>=0.29

deepagents>=0.6
langchain>=1.0
python-dotenv>=1.0

# Model provider matching MODEL=openrouter:* (swap if MODEL changes):
langchain-openai>=0.2
