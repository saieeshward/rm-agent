# Runtime deps for the deployed web app (no Playwright/Chromium — the hosted DB
# is loaded from the cached snapshot, so the container never scrapes).
psycopg[binary]>=3.1
psycopg_pool>=3.2
pyyaml>=6.0

fastapi>=0.110
uvicorn>=0.29

deepagents>=0.6
langchain>=1.0
python-dotenv>=1.0

# Model providers — all installed so the in-app model switcher can resolve any of
# them at runtime (each is used only when the selected model needs it, and only if
# its API key env var is set):
langchain-openai>=0.2          # openrouter:* and openai:*
langchain-google-genai>=2.0    # google_genai:*  (GOOGLE_API_KEY)
langchain-anthropic>=0.3       # anthropic:*      (ANTHROPIC_API_KEY)
